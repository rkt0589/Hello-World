// Very Basic Hello world program that can be compiled and run on different system providing compiled with appropriate toolchain(compiler

#include <stdio.h>                  // standard I/O header file
 
int main(int argc, char **argv)
{
     printf("\nHello World !!!\n");        // print "Hello World" message on screen
     return(0);
}
